import 'intersection-observer';

// Add your polyfills
// This files runs at the very beginning (even before React and Next.js core)

process.env.NODE_ENV !== 'production' && console.log('Load your polyfills');
